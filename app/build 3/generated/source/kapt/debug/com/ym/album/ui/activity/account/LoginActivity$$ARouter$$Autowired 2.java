package com.ym.album.ui.activity.account;

import com.alibaba.android.arouter.facade.service.SerializationService;
import com.alibaba.android.arouter.facade.template.ISyringe;
import com.alibaba.android.arouter.launcher.ARouter;
import java.lang.Object;
import java.lang.Override;

/**
 * DO NOT EDIT THIS FILE!!! IT WAS GENERATED BY AROUTER.
 */
public class LoginActivity$$ARouter$$Autowired implements ISyringe {
  private SerializationService serializationService;

  @Override
  public void inject(Object target) {
    serializationService = ARouter.getInstance().navigation(SerializationService.class);
    LoginActivity substitute = (LoginActivity)target;
    substitute.telephone = substitute.getIntent().getExtras() == null ? substitute.telephone : substitute.getIntent().getExtras().getString("telephone", substitute.telephone);
    substitute.password = substitute.getIntent().getExtras() == null ? substitute.password : substitute.getIntent().getExtras().getString("password", substitute.password);
  }
}
